const islandCount = (grid) => {
    // todo
    let graph={w:[],
        
    }
    for (let i of grid){
        if(!graph[i])graph[]
    }
    
  };



  const grid = [
    ['W', 'L', 'W', 'W', 'W'],
    ['W', 'L', 'W', 'W', 'W'],
    ['W', 'W', 'W', 'L', 'W'],
    ['W', 'W', 'L', 'L', 'W'],
    ['L', 'W', 'W', 'L', 'L'],
    ['L', 'L', 'W', 'W', 'W'],
  ];
  
  islandCount(grid); // -> 3